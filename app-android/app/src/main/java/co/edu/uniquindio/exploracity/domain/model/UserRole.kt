package co.edu.uniquindio.exploracity.domain.model

/** Rol de la sesión, resuelto al iniciar sesión. El moderador usa la misma app con una pestaña más. */
enum class UserRole {
    USER,
    MODERATOR,
}
